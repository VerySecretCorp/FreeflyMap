{
  "name": "geohash",
  "version": "1.0.0",
  "description": "",
  "source": "site/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "parcel watch site/index.html --public-url ./",
    "build": "parcel build site/index.html --public-url ./",
    "deploy": "parcel build site/index.html --public-url ./ && push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@googlemaps/markerclusterer": "^2.5.3",
    "ngeohash": "^0.6.3",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "process": "^0.11.10",
    "push-dir": "^0.4.1"
  }
}
